# ADR-0003: Use-Case-Agnostic Baseline

Status: Accepted

## Decision

Baseline v0.1 contains no selected finance workflow. The only action names used for implementation conformance are test-only `conformance.*` actions.

## Reason

A domain integration should be chosen because it has a real agentic control problem, not because an adjacent repository exists.
