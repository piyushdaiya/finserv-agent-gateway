# Workflow Packs

This directory is reserved for reusable workflow packs after the core governance kernel is implemented.

Baseline v0.1 intentionally contains no real financial-services workflow pack.

A test-only conformance adapter/action vocabulary may exist under test code in a future implementation, but it is not a product workflow.
